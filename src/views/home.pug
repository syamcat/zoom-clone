doctype html
html(lang="en")
	head
		meta(charset="UTF-8")
		meta(name="viewport", content="width=device-width, initial-scale=1.0")
		title noom
		link(rel="stylesheet", href="https://unpkg.com/mvp.css")
	body 
		header
			h1 Noom
		main
			div#welcome
				form
					input(placeholder="room name", required, type="text")
					button Enter Room
		//- 아래 스크립트 선언 순서가 바뀌지 않아야 ReferenceError: Can't find variable: io 가 발생하지 않는다
		script(src="/socket.io/socket.io.js") <!-- socket.io import -->
		script(src="/public/js/app.js")